{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capability for the staged LabelPilot Rust runtime",
  "windows": ["main"],
  "permissions": ["core:default"]
}